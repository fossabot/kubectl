name: test kubectl

on:
  push:

jobs:
  tests:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v1
    - name: test alias
      run: make test
    - name: ls
      run: make test-hello
